# Variables para docker-compose (todas tienen default — el compose levanta sin .env)
DB_USER=postgres
DB_PASSWORD=changeme
DB_NAME=ecommerce
JWT_SECRET=changeme
# URL del API vista desde el navegador (host), usada en build del front
VITE_SERVER_URL=http://localhost:4000
