# Backlog de tickets

> Fuente de verdad del trabajo del proyecto. Cada ticket se implementa como un **OpenSpec change**
> (`/opsx:propose TK-###`): el workflow lee este documento, toma el contexto del ticket, y al
> archivar el change el ticket pasa a `closed` con el enlace al change.
>
> **Categorías**: `requerimiento` = pedido explícito del challenge (PDF) · `técnico` = modificación
> del proyecto necesaria/deuda que no es un requerimiento del challenge.
>
> Convención: el nombre del OpenSpec change es el slug del ticket (ej. TK-007 → `products-crud`).

| # | Título | Categoría | Descripción / contexto | Estatus | OpenSpec change |
|---|---|---|---|---|---|
| TK-001 | Baseline y control de versiones | técnico | Commit inicial de los templates `api/` y `web/` + spec previo (`docs/initial.md`) para poder recuperar cualquier archivo borrado durante la limpieza. | closed | — (pre-OpenSpec) |
| TK-002 | Instalar OpenSpec a nivel proyecto | técnico | Workflow spec-driven ([openspec.dev](https://openspec.dev/)) con comandos `/opsx:*` y skills en `.claude/`, artefactos en español. Todas las features nuevas pasan por propose → apply → archive. | closed | — (pre-OpenSpec) |
| TK-003 | Análisis de templates base | técnico | Auditoría de `api/` (NestJS+TypeORM contaminado con dominio fiscal SAT) y `web/` (Minimals UI Kit v6 full). Veredicto de reutilización documentado en [02-analisis-base.md](02-analisis-base.md). | closed | — (pre-OpenSpec) |
| TK-004 | Poda del backend | técnico | Eliminar módulos fiscales, DynamoDB, integraciones (Twilio/OpenAI/Google), código muerto y 10 deps. Arreglos: Dockerfile roto (yarn→npm ci), swagger v11→v7, JWT via ConfigService, flags `DB_SSL`/`DB_SYNC`, moduleNameMapper de Jest. | closed | — (pre-OpenSpec) |
| TK-005 | Poda del frontend | técnico | De ~1334 a 500 archivos: solo product/checkout/order/error/address + auth JWT + componentes genéricos. −28 deps (−420 paquetes). Dockerfile nginx SPA creado. Puerto dev 8080→3000. | closed | — (pre-OpenSpec) |
| TK-006 | Ambiente Docker + desarrollo local | requerimiento | `docker-compose.yml` único en raíz: db (Postgres 16, healthcheck) + api (4000→8080, prefijo `api/v1`) + web (3000→nginx 80). Verificado FE→BE→DB. Flujo local manual documentado en README. | closed | — (pre-OpenSpec) |
| TK-007 | CRUD de productos (BE + FE) | requerimiento | **BE**: la entidad actual (`title`, `slug`, `sizes`, `gender`, precio `float`) no coincide con el CSV del challenge. Rediseñar a `sku` (UNIQUE), `name`, `description`, `category` (default "Uncategorized"), `price DECIMAL(10,2)`, `stock` int ≥0, `weight_kg DECIMAL` nullable; implementar `update()`/`remove()` (hoy son stubs) + `@ApiProperty` en DTOs. **FE**: conectar el DataGrid admin y el formulario RHF+zod a los endpoints reales (create/edit/delete con `mutate` de SWR — hoy el submit es `setTimeout`+toast); recortar `IProductItem` y `NewProductSchema` al contrato real. Referencia: initial.md §3. Incluyó además: Storybook + Playwright e2e (5/5), Swagger con ejemplos, tests unitarios BE (10/10), dashboard público vía `CONFIG.auth.skip`, y la parte de mappers/endpoints de TK-011 para product. | closed | [products-crud](../openspec/changes/archive/2026-08-26-products-crud/) |
| TK-008 | Búsqueda de productos (BE + FE) | requerimiento | **BE**: `findAll` no acepta filtros. Añadir `?q=` (ILIKE sobre name/description/sku), filtro por categoría, rango de precio, orden whitelisted, sobre la paginación existente (`PAGINATION_GUIDE.md`); índices adecuados. **FE**: conectar `product-search` (autocomplete), filtros y orden del shop a los query params reales; paginación server-side en listado y DataGrid. Referencia: initial.md §6 (CQS ligero). **Parcial (rama TK-031)**: hecho el `?q=` server-side sobre name/sku/description/category + filtro `category` + índices trigram, y el buscador del DataGrid admin. **Deuda restante (2026-08-28)**: BE → `minPrice`/`maxPrice`, `inStock`, `sortBy`/`sortDir` whitelisted, `category` multi-valor y `GET /products/categories` (se hace en TK-039); FE → filtros y orden de la tienda pública (TK-035) y del dashboard (TK-036). | closed | [product-filters-api](../openspec/changes/archive/2026-08-28-product-filters-api/) + [dashboard-product-filters](../openspec/changes/archive/2026-08-29-dashboard-product-filters/) |
| TK-009 | Módulo de importación CSV (BE + FE) | requerimiento | **BE**: no existe nada — instalar multer + `csv-parse`. Endpoint `POST /products/import` (multipart), import **parcial** (no todo-o-nada), upsert por SKU (insert / no-op / update+warning), tabla `import_batches` con status, contadores y reporte de errores por fila (fila, columna, motivo). **FE**: pantalla de import en el dashboard — upload con validación UX (extensión .csv, tamaño máx), y render del resumen del batch (insertados / actualizados / rechazados con detalle por fila). Las reglas de validación del contenido viven en TK-023. Referencia: initial.md §4 y §4.5. | closed | [csv-import](../openspec/changes/archive/2026-08-27-csv-import/) |
| TK-010 | Órdenes con control de stock (BE + FE checkout) | requerimiento | **BE**: reescribir orders sobre Postgres (el de DynamoDB se eliminó): entidades `Order`/`OrderItem` con `unit_price_snapshot`, transacción con `SELECT ... FOR UPDATE` (initial.md §5), `total` recalculado en servidor, `idempotency_key` UNIQUE, 409 si no hay stock. **FE**: el checkout del template hace POST real a `/orders` al confirmar (hoy no persiste nada), genera el idempotency key al iniciar el flujo, maneja el 409 (sin stock) con UX clara, y el listado/detalle de órdenes del dashboard lee del API. El cobro lo resuelve TK-022. | closed | [orders-and-fake-payment](../openspec/changes/archive/2026-08-29-orders-and-fake-payment/) |
| TK-011 | Capa de integración FE ↔ API (base) | técnico | Fundación que usan TK-007/008/009/010: alinear `endpoints` en `src/utils/axios.ts` a las rutas reales `api/v1/*` (hoy el front llama al mock Minimals → 404), crear los **mappers/adapters** por dominio (`product.mapper.ts`: API DTO ⇄ tipo de UI, conversión DECIMAL-string→number en un solo lugar — patrón ACL decidido en conversación), tipos del envelope de paginación `{ data, pagination }`, quitar imports de `src/_mock`, y proxy `/api` en Vite para dev. (La migración SWR → React Query ya se completó: `product` y `status` siguen el patrón facade y SWR está desinstalado — este ticket solo cambia URLs/shapes a las del API real y añade las mutaciones.) | closed | [products-crud](../openspec/changes/archive/2026-08-26-products-crud/) |
| TK-012 | Seed automático vía pipeline de import | requerimiento | Al levantar docker, el api debe importar el CSV de ejemplo **a través del pipeline de import real** (así el seed prueba el import end-to-end, initial.md §10.3). Cero pasos manuales para ver datos. Depende de TK-009. | closed | [migrations-and-seed](../openspec/changes/archive/2026-08-27-migrations-and-seed/) |
| TK-013 | Migraciones TypeORM reales | técnico | Hoy el schema se crea con `DB_SYNC=true` (temporal). Crear `data-source.ts`, carpeta `migrations/`, scripts npm, correr migraciones al iniciar el contenedor, y apagar `DB_SYNC`. Idealmente junto con TK-007 (el schema nuevo nace como migración). | closed | [migrations-and-seed](../openspec/changes/archive/2026-08-27-migrations-and-seed/) |
| TK-014 | Exception filter global + contrato de errores | técnico | `handleDBExceptions` está copiado en cada servicio. Crear `common/filters` con el shape de error consistente de initial.md §7 (`statusCode`, `error`, `message`, `path`, `timestamp`). | closed | [global-error-contract](../openspec/changes/archive/2026-08-29-global-error-contract/) |
| TK-015 | Hardening de seguridad | técnico | `helmet`, rate limiting (`@nestjs/throttler`) en import, CORS explícito al origin del web (hoy `*`), sanitización anti-XSS de `name`/`description` en el DTO de import (el escape de React no basta como única defensa). Referencia: initial.md §8, §10.6. | closed | (sin change: hardening directo — helmet, CORS explícito, rate limit del import; el anti-XSS ya lo cubría `@NoHtml`) |
| TK-016 | Estrategia de tests | técnico | Cobertura actual 0%. Prioridad: unitarios del pipeline de validación CSV (casos reales del archivo), concurrencia de stock (dos compras del último ítem), e2e import→search→order. FE: Vitest + Testing Library si el tiempo alcanza. | closed | (sin change: [docs/testing/STRATEGY.md](testing/STRATEGY.md)) |
| TK-017 | README final de entrega | requerimiento | El challenge pide: decisiones/approach/alternativas consideradas, fecha de descarga del CSV (2026-08-26 ✅ ya está), instrucciones de ejecución, y quitar comentarios del código generado con AI. Revisión final pre-entrega. | closed | (sin change: revisión final del README) |
| TK-019 | Decidir: conservar o eliminar auth/users del API | técnico | El challenge no pide auth (initial.md §10.2). Se conservaron `auth/` y `users/` como punto de extensión funcional. Alternativa: eliminarlos para un repo más minimal. **Decisión tomada (2026-08-27): auth SE QUEDA** y pasa a uso activo — TK-030 lo usa para atribuir imports (`imported_by`). | closed | — (decisión, implementación en TK-030) |
| TK-020 | Redis en el stack + página de estado en el FE | técnico | Añadir Redis al docker-compose y al API (cliente + config). Dos endpoints demostrativos: `GET /status/redis` (dato leído/escrito en Redis: contador de visitas + latencia) y `GET /status/db` (dato leído de Postgres: count de productos + NOW() + latencia). Página `/status` en el FE que muestra configuración del web y tarjetas en vivo de API (`/health`), Postgres y Redis, evidenciando qué está funcionando y de qué fuente viene cada dato. | closed | [redis-status-page](../openspec/changes/archive/2026-08-26-redis-status-page/) |
| TK-021 | Decidir: uso real de Redis (caché de búsqueda vs carrito server-side) | técnico | TK-020 deja Redis operativo pero solo con endpoints demostrativos. Candidatos de uso real: (a) caché de resultados de búsqueda/listado de productos con invalidación al mutar, (b) carrito de compras server-side (hoy vive en localStorage del FE). **Decisión tomada (2026-08-28): el carrito SE QUEDA en localStorage** — no protege nada (la verdad del stock se resuelve al crear la orden con `SELECT ... FOR UPDATE` en TK-010), y moverlo a Redis solo añade carrito anónimo por cookie, TTL y estado duplicado FE/BE. Redis se queda con el uso (a), caché de listado/búsqueda, que es el que sí paga con la tienda pública pegando al API en cada tecleo. Implementación en TK-038. | closed | — (decisión) |
| TK-030 | Usuario seed + atribución de imports (imported_by) | técnico | Cierra el gap "el batch no guarda quién importó": seed idempotente de un usuario admin al boot (password vía `SEED_ADMIN_PASSWORD`), `POST /products/import` protegido con el guard JWT existente, columna `imported_by` en `import_batches` (migración) — el seed de catálogo firma como `seed`. FE: página de import pide sesión si no la hay (sign-in JWT del template conectado a los endpoints reales), historial y detalle muestran "Imported by". e2e autenticados. Resuelve la decisión de TK-019. | closed | [auth-login-and-route-protection](../openspec/changes/archive/2026-08-29-auth-login-and-route-protection/) |
| TK-033 | Regla: un SKU a lo sumo una vez por archivo de import | requerimiento | Corrección de la decisión de initial.md §1.4 tras revisar el CSV: el upsert secuencial ("gana la última fila") hacía que el resultado dependiera del **orden** de las filas y elegía en silencio entre datos financieros contradictorios (RS-001 $89.99 vs $94.99). Nueva regla: si un SKU se repite dentro del archivo se rechazan todas sus filas, reportando líneas y si el conflicto era por datos contradictorios o copias idénticas. Implementado con pipeline en **dos fases** (validar archivo completo → aplicar), alineado con el `ON CONFLICT` de Postgres y el `MERGE` del estándar SQL. El upsert contra la base (SKU ya existente) no cambia: ahí el archivo sí tiene autoridad. | closed | (rama TK-031) |
| TK-032 | UX del import CSV: navegación, métricas explicadas y tablas de resultado | técnico | Refinamiento pedido tras probar la app: (a) el import vive dentro del dropdown de Product con dos entradas —importar e historial—; (b) cada tarjeta del resumen lleva icono y explicación de qué significa la métrica; (c) sustituir el recuadro amarillo de warnings por tablas: una de filas con incidencia (rechazadas en rojo, actualizadas en ámbar, con iconos y color de fila) y otra con las filas creadas. Implica extender el `ImportResult` del BE para incluir las filas creadas, que hoy no se devuelven. | closed | (dentro de la rama TK-031) |
| TK-031 | Login funcional + protección de rutas (BE + FE) | requerimiento | Conectar el flujo de sesión end-to-end con el usuario demo sembrado por migración (`demo@demo.com` / `demo`) y proteger lo que muta el catálogo. Decisión del usuario: **las compras siguen públicas** (un cliente compra sin cuenta); se protege el alta/gestión de productos, el import CSV y su historial, el status y el CRUD de usuarios. BE: guard JWT reutilizable en `common/guards`. FE: `AuthGuard` del template activado en `/dashboard/*` (hoy desactivado con `CONFIG.auth.skip`), sign-in contra el API real, token en el interceptor de axios y manejo de 401. **Absorbe TK-030** (columna `imported_by` + "Imported by" en el historial de imports). | closed | [auth-login-and-route-protection](../openspec/changes/archive/2026-08-29-auth-login-and-route-protection/) |
| TK-029 | Historial de import batches en el dashboard (FE) | técnico | Cierra el gap detectado en la revisión del pipeline: la auditoría de batches existe en BE (`import_batches` + endpoints paginados) pero no había vista. Lista `/dashboard/product/import/batches` (DataGrid server-side: archivo, status, contadores, fecha) + detalle por batch reutilizando ImportSummary/ImportRejectedTable/warnings del flujo post-upload. Accesos desde la página de import. Solo FE. | closed | [import-batches-history](../openspec/changes/archive/2026-08-27-import-batches-history/) |
| TK-028 | Política de contenido HTML: rechazar, no sanitizar | requerimiento | Corrección del usuario a la decisión de initial.md §1.3: guardar `alert('xss')` tras el strip de tags sigue siendo alterar silenciosamente el dato original (inconsistente con la regla del precio "free") y deja basura que un consumidor sin escape (dangerouslySetInnerHTML, email, PDF) trataría como HTML. Nueva política: name/description con markup HTML → la fila se RECHAZA reportando el campo inválido (import) / 400 (CRUD). Espejo en zod del FE. Cambian los números del fixture: línea 20 rechazada → 87 insertados, 5 rechazadas. | closed | — (policy change) |
| TK-027 | Limpieza de UI del dashboard + Status como sección | técnico | Eliminar elementos del template que no se usan (detectados en revisión visual del usuario): workspace switcher "Team 1", tarjeta de usuario demo "Jaydon Frankie" + botón Upgrade to Pro, popover de cuenta con menú falso (Home/Profile/Projects/Subscription) y banner 35% OFF, notificaciones/contactos/selector de idioma con datos mock, búsqueda global ⌘K. Mover la página Status del nav público a su propia sección del dashboard (`/dashboard/status`).  **Revertido parcialmente por TK-052 (2026-08-31)**: el usuario pidió recuperar la búsqueda de páginas. El resto de la purga sigue en pie. | closed | — (UI cleanup) |
| TK-025 | Refactor: import y seed como módulos de primer nivel | técnico | Corrección del usuario: el approach es de MÓDULOS, no submódulos — `products/import/` se mueve a `modules/import/` (alineado con el módulo `catalog-import` que initial.md §0 ya definía) y el seed sale de `database/` (que solo debe contener cosas de DB: conexiones, migraciones, data-source) hacia `modules/seed/`. Las rutas HTTP no cambian (`/products/import`) — organización de código ≠ contrato de API. Docs de arquitectura actualizados. | closed | — (refactor + docs) |
| TK-026 | Import robusto ante CSV arbitrario (adversarial) | requerimiento | El requerimiento es "products can also be imported from a csv" — cualquier archivo, no solo el ejemplo. Endurecer y probar: headers faltantes/extra/desordenados → 400 claro; CSV malformado (comillas sin cerrar) → 400, nunca 500; binario renombrado .csv; archivo solo-headers (batch válido con 0 filas); filas con columnas de más/menos; valores maliciosos en cualquier celda (ya cubierto por DTO); encoding con BOM. Tests para cada caso. | closed | — (dentro de TK-025) |
| TK-024 | Documentar arquitectura del backend | técnico | Análisis y documentación de la arquitectura por capas del api (config → database/infra → common → modules de dominio), espejo de lo hecho para el FE: skill `be-architecture` para agentes, sección en README, resumen en CLAUDE.md. Incluye la racional del seed en `database/` (bootstrap de infra que orquesta al dominio) y la política de archivos subidos (memoria, sin disco; auditoría en import_batches). Limpieza: products.json muerto eliminado. | closed | — (docs-only) |
| TK-022 | Fake payment provider (patrón Strategy) | requerimiento | El challenge pide explícitamente simular el pago. Interfaz `PaymentProvider` con implementación `FakePaymentProvider` (initial.md §6): el servicio de orders la consume por inyección, de modo que conectar Stripe/otro mañana sea implementar la interfaz sin tocar orders. El fake valida el "cobro" dentro de la misma transacción de la orden (si el pago falla, la orden y el descuento de stock se revierten). En el FE, el paso de pago del checkout usa el método fake y muestra el resultado. Depende de TK-010. | closed | [orders-and-fake-payment](../openspec/changes/archive/2026-08-29-orders-and-fake-payment/) |
| TK-023 | Validaciones del CSV (pipeline por capas) | requerimiento | El corazón del challenge — cada fila del CSV pasa por la defensa en profundidad de initial.md §4.5: (2) MIME type y tamaño en controller, (3) headers/encoding/comillas en el parser, (4) DTO por fila con `class-validator` + `@Transform` (limpiar `$` del precio, `trim`, sanitización anti-XSS de name/description), (5) reglas de negocio (SKU duplicado dentro del archivo y contra la DB), (6) constraints en Postgres (`UNIQUE(sku)`, `CHECK(price >= 0)`, `NOT NULL`). Casos reales a cubrir con tests: XSS (línea 20), SQLi (29), `$29.99` (4), `"free"` (7), stock −5 (16), name vacío (25) y solo espacios (41), filas vacías (62-63), duplicados RS-001/BS-021, `price 0.00` válido (47), categoría vacía → "Uncategorized" (52), comillas/comas escapadas (53, 59), Unicode (31). Cada decisión ya está tomada en initial.md §1 — implementarla tal cual. Depende de TK-009. | closed | [csv-import](../openspec/changes/archive/2026-08-27-csv-import/) |
| TK-034 | Limpieza de UI de auth (FE) | técnico | Pedido tras probar la app. (a) **Ocultar el registro sin borrarlo**: `sign-up` sale del router (`<Navigate to={signIn} replace />`), se quita el link "Get started" del sign-in; `pages/auth/jwt/sign-up.tsx` y `jwt-sign-up-view.tsx` se conservan intactos y `paths.auth.jwt.signUp` sigue declarado. (b) Quitar el link "Need help?" del header de auth (`helpLink: false` en `auth-split`, ya apagado en main y dashboard). (c) **Rediseño del sign-in**: cambiar de `AuthSplitLayout` a `AuthCenteredLayout` —que ya existe en el repo sin usar— con lo que desaparecen el sidebar izquierdo y el icono JWT; añadir link para volver al home. Se apila sobre TK-031, que toca los mismos archivos. | closed | [public-storefront](../openspec/changes/archive/2026-08-29-public-storefront/) |
| TK-035 | Home pública: tienda en `/` con iconos por categoría (FE) | requerimiento | Hoy `/` es un `<Navigate to="/product">` y `pages/product/list.tsx` pide `{ page: 1, limit: 24 }` hardcodeado, sin paginación ni buscador. (a) `/` **se convierte** en la tienda (hero compacto + buscador + chips de categoría + grid + paginación server-side); `/product` redirige a `/`; detalle y checkout no cambian. (b) Botón de login en el header **condicional a la sesión** (sin sesión → "Sign in"; con sesión → `session-menu` + Dashboard; hoy "Dashboard" se muestra siempre y el guard rebota). (c) Componente **icono por categoría**: `category` es `varchar(100)` libre con default `Uncategorized`, así que es un mapa normalizado (trim+lowercase) → icono de Iconify **con fallback obligatorio**, alimentado por `GET /products/categories`; reemplaza el `placeholder.svg` que hoy se repite en todas las tarjetas. (d) Filtros y orden viviendo en la URL (`useSearchParams`) para back-button y links compartibles. Depende de TK-039. | closed | [public-storefront](../openspec/changes/archive/2026-08-29-public-storefront/) |
| TK-036 | Filtros y orden del dashboard (FE + BE menor) | técnico | (a) **Bug**: el DataGrid de productos tiene `paginationMode="server"` pero no `sortingMode="server"`, así que ordenar por una columna ordena solo los 10 registros de la página visible sobre 87 — parece que funciona y miente. Añadir `sortingMode="server"` conectado a `sortBy`/`sortDir` de TK-039, y la columna **"Updated at"** al DataGrid: el objetivo declarado del usuario es verificar imports, y como el import hace upsert por SKU una fila actualizada no mueve su `createdAt` — solo ordenando por `updatedAt` se ve lo que un segundo import tocó. (b) Toolbar de filtros **explícita** (categoría, rango de precio, en stock) reutilizando `components/filters-result/` para los chips de filtro activo. Se descarta el panel de filtros nativo de MUI (`filterMode="server"`): su `GridFilterModel` manda una gramática abierta de operadores por columna que habría que traducir a SQL, lo que es construir un motor de queries genérico. (c) Buscador en el historial de import batches, que no tiene ninguno — requiere `?q=` por `filename` en el endpoint de batches. Se descarta revivir la búsqueda global ⌘K que quitó TK-027 (corría sobre mocks; hacerla de verdad es búsqueda cross-entity, otro ticket). Depende de TK-039.  **Matizado por TK-052 (2026-08-31)**: ese descarte valía para la búsqueda de contenido, no para la de páginas — los items del nav son datos reales, no mocks. | closed | [dashboard-product-filters](../openspec/changes/archive/2026-08-29-dashboard-product-filters/) |
| TK-037 | Carrito en el navbar, mini-cart y recibo en PDF (FE) | requerimiento | (a) Hoy `CartIcon` es una píldora `position: fixed` montada **solo** dentro de `ProductShopView`, así que desaparece en el detalle de producto. Pasa al `HeaderBase` como botón con badge → **drawer de mini-carrito** (mismo patrón que el `SettingsButton`) con las líneas, cantidades y botón para ir al checkout. `CheckoutProvider` ya vive en `app.tsx`, así que el header lo lee sin mover el provider. (b) Sustituir el icono `carbon:bicycle` de la opción de envío "Free" en `checkout-delivery.tsx`, y de paso mover el icono dentro de `DELIVERY_OPTIONS` (hoy se elige comparando `label === 'Free'`, frágil por depender de un string de UI). (c) **Descarga del recibo en PDF**: `onDownloadPDF` es literalmente un no-op en `checkout-view.tsx`. `@react-pdf/renderer` cargado con `import()` dinámico para que su peso no toque el bundle principal. Firma estable `downloadReceipt(receipt)`: hoy el recibo se arma del estado del `CheckoutContext`, con TK-010 se armará de la orden real sin tocar el generador. Sustituir el id de orden hardcodeado por uno generado en cliente al iniciar el checkout, que es el `idempotency_key` que TK-010 necesita. | closed | [public-storefront](../openspec/changes/archive/2026-08-29-public-storefront/) |
| TK-038 | Caché Redis del listado/búsqueda de productos (BE) | técnico | Implementación de la decisión de TK-021: darle a Redis un uso real en vez de los endpoints demostrativos de TK-020. Cachear la respuesta de `GET /products` por combinación de query params, con invalidación al crear/actualizar/borrar producto y al cerrar un batch de import. Cobra sentido junto con TK-035, que hace que la tienda pública pegue al API en cada tecleo del buscador. Prioridad baja: no bloquea ninguna entrega del challenge. | closed | [product-listing-cache](../openspec/changes/archive/2026-08-29-product-listing-cache/) |
| TK-039 | Filtros y orden de productos en el API (BE) | requerimiento | Cierra la deuda BE de TK-008 y desbloquea TK-035 y TK-036, que si no tendrían que tocar el mismo DTO en paralelo. `ProductFiltersDto` gana `minPrice`/`maxPrice` (rango, validado `min <= max`), `inStock` (booleano → `stock > 0`), `sortBy`/`sortDir` con **whitelist estricta** (`name·price·stock·createdAt` × `asc·desc`, default `createdAt desc` como hoy) y `category` multi-valor (`?category=Electronics,Tools`, hoy es exacto de un solo valor). Nuevo `GET /products/categories` (público) con las categorías distintas y su conteo: lo consumen el dropdown de filtro del dashboard, los chips del home y el mapa de iconos por categoría. Migración con índice btree en `price`. Swagger con ejemplos + tests unitarios del builder de query (incluye que un `sortBy` fuera de la whitelist no llegue nunca al SQL). | closed | [product-filters-api](../openspec/changes/archive/2026-08-28-product-filters-api/) |
| TK-040 | Revisión rápida del import: columnas completas en Created rows | técnico | Pedido tras probar el import: la tabla de filas creadas solo mostraba línea, SKU y nombre, insuficiente para revisar de un vistazo qué entró al catálogo. El BE solo devolvía `{ line, sku, name }` por fila creada, así que se amplió `ImportCreatedRow` con `description`, `category`, `price`, `stock` y `weightKg`, tomados de la entidad ya persistida — es decir, con los valores **normalizados** (categoría vacía → `Uncategorized`, precio con dos decimales, nulls donde el CSV traía celdas vacías), que es lo que de verdad quedó guardado. Los campos son opcionales en el tipo del FE para que los batches guardados antes de este cambio sigan abriéndose, pintando `—`. Incluye homologar la columna del nombre de producto como **"Name"** en las tres tablas que la llamaban distinto (Created rows, lista de productos y carrito del checkout). | closed | [dashboard-product-filters](../openspec/changes/archive/2026-08-29-dashboard-product-filters/) |
| TK-041 | Explorar y comparar en la lista de productos: descripción, anchos y búsqueda múltiple | técnico | Pedido tras usar la pantalla para revisar imports. (a) **Columna `Description`** después de `Name`, con truncado y tooltip: no estaba y es el campo que más falta hace para revisar de un vistazo qué entró. (b) `Name` dejaba de ser elástica (`flex: 1` se comía la fila) y pasa a ancho fijo, cediendo el espacio elástico a `Description`; además los **anchos y la visibilidad de columnas se persisten** en localStorage vía `onColumnWidthChange` —antes se perdían al navegar— con botón "Reset layout" cuando hay ajustes guardados. (c) **Búsqueda de varios términos**: el buscador ya no aplica al teclear sino al pulsar Enter, y cada término se vuelve un chip borrable; el API acepta `q` repetido (`?q=a&q=b`) y los **une con OR** — el caso de uso es "enséñame estos productos", no "filas que cumplan todos". Se eligió parámetro repetido en vez de separar por comas como `category` porque un término de búsqueda libre sí puede contener comas. | closed | [dashboard-product-filters](../openspec/changes/archive/2026-08-29-dashboard-product-filters/) |
| TK-042 | Reporte del import: estados honestos, filas omitidas y filtro | técnico | Pedido tras revisar el resultado de un import. (a) La tarjeta se llamaba **"Rows with issues"** pero incluía las filas *actualizadas*, que no son una incidencia sino un aviso; pasa a **"Rows to review"** con un subtítulo que enumera los tres estados. (b) Las **filas en blanco solo se contaban**: el BE descartaba su número de línea, así que no había forma de saber cuáles se omitieron. `ImportBatchReport` gana `skipped: [{ line }]` y aparecen en la tabla con su propio estado. (c) **Columna `Name` antes de `SKU`** — requiere que el BE incluya el nombre en las filas rechazadas y actualizadas, que hoy solo llevaban `{ line, sku, errors }`; se guarda el valor **crudo del archivo**, de modo que una fila rechazada por su nombre muestre exactamente qué tenía de malo (React lo escapa, el markup se ve como texto). (d) **Filtro** por estado y por texto sobre línea/SKU/nombre/motivo, con contador "Showing N of M", en la línea del filtro de la lista de productos. `skipped` es opcional en el tipo del FE para que los batches guardados antes sigan abriéndose. | closed | [dashboard-product-filters](../openspec/changes/archive/2026-08-29-dashboard-product-filters/) |
| TK-043 | Homologar estados del reporte con las tarjetas indicadoras | técnico | Nota del usuario al revisar el resultado de un import: los badges de estado de la tabla usan icono y color propios, distintos de los de las seis tarjetas de arriba (Created, Updated, Rejected, Skipped empty). Unificar icono y color por estado entre tarjeta y badge, y llevar esos mismos iconos a la leyenda del pie de la tabla, que hoy es solo texto. | closed | [import-report-consistency](../openspec/changes/archive/2026-08-29-import-report-consistency/) |
| TK-044 | Homologar el orden de columnas: SKU antes que Name | técnico | Nota del usuario: la lista de productos muestra `SKU` y luego `Name`, mientras que Rows to review quedó con `Name` antes de `SKU`. **Corrige la decisión de TK-042**, que puso Name primero a petición previa; la referencia pasa a ser la tabla de productos. Aplicar en las tres tablas (lista de productos, Created rows, Rows to review) para que la lectura sea la misma en todas. | closed | [import-report-consistency](../openspec/changes/archive/2026-08-29-import-report-consistency/) |
| TK-045 | Filtros en la tabla Created rows | técnico | Nota del usuario: con 85 filas creadas no hay forma de buscar dentro de la tabla. Añadir el mismo filtro de texto que se usa en Rows to review y en la lista de productos, conservando el header actual (`Created rows (85)` + subtítulo). Idealmente extraer el bloque de filtro a un componente compartido en vez de repetirlo por tercera vez. | closed | [import-report-consistency](../openspec/changes/archive/2026-08-29-import-report-consistency/) |
| TK-046 | Layout de las tablas del dashboard: densidad, controles y header | técnico | Notas del usuario sobre `/dashboard/product`: (a) el botón de columnas (icono de ojo) está en una fila propia que solo genera espacio muerto — subirlo junto a los filtros; (b) mostrar al menos **20 filas** por página por defecto en todas las tablas, no 10; (c) eliminar el hueco vacío bajo la tabla cuando hay menos registros que la altura fija del grid; (d) el título de la página es solo `List`, poco descriptivo — darle un nombre acorde y homologarlo con la entrada del dropdown de navegación. | closed | [dashboard-grid-layout](../openspec/changes/archive/2026-08-29-dashboard-grid-layout/) |
| TK-047 | Contrato del reporte: `name` siempre presente en las filas rechazadas | técnico | Detectado en la captura del usuario tras TK-042 y confirmado en el JSON del import: las filas rechazadas por la regla de SKU duplicado (líneas 2, 11, 36, 56, 89 del CSV de ejemplo) llegan sin `name` aunque el archivo sí lo trae, porque `rejectDuplicateSkus` construye la fila con `{ line, sku, errors }` y no propaga `dto.name`, a diferencia de las otras dos rutas de rechazo. **El problema de fondo es el contrato, no la línea que falta**: hoy un `name` ausente significa dos cosas opuestas —que la fila no tenía nombre (líneas 25 y 41, correcto y semántico) o que el backend olvidó mandarlo (el bug)— y el FE pinta `—` en ambos casos, así que el fallo es indistinguible del comportamiento correcto desde la pantalla. **Decisión**: (a) mandar siempre el campo, con cadena vacía cuando la fila no tenía nombre —`""` es más fiel que `null`, la celda no era desconocida sino vacía—, de modo que "ausente" pase a ser una violación del contrato y no un estado legítimo; (b) quitar el `?` de `name` en `ImportRejectedRow` e `ImportWarning` del BE, porque ese opcional es justo lo que permitió que la ruta de duplicados compilara sin el campo: con el tipo obligatorio el compilador cuenta las rutas por nosotros y esta clase de bug deja de compilar; (c) mantener `name?` **opcional en el tipo del FE**, asimetría deliberada —estricto al escribir, tolerante al leer— porque los batches ya guardados no lo llevan y su detalle debe seguir abriéndose. Mismo razonamiento aplicable a `sku?`, que sufre la misma ambigüedad. | closed | [import-report-name-contract](../openspec/changes/archive/2026-08-29-import-report-name-contract/) |
| TK-048 | Órdenes reales en el dashboard + purga del mock de plantilla | requerimiento | Detectado al probar la app: se realiza una compra, el API la persiste correctamente (`orders` + `order_items`, status `PAID`, `payment_reference`, stock descontado) y `/dashboard/order` sigue mostrando las 20 órdenes ficticias de `src/_mock/_order.ts` con clientes inventados y estados (`Completed`/`Cancelled`/`Refunded`) que no existen en el dominio. Fue un aplazamiento consciente de TK-010/TK-022 —`types/purchase.ts` lo documenta— que dejó `getPurchases()`/`getPurchase()` escritas pero sin consumidor, aun cuando `GET /api/v1/orders` y `GET /api/v1/orders/:id` ya existen, están paginados y protegidos con JWT. **No requiere cambios de backend.** (a) Vistas de lista y detalle sobre `IPurchase`, con solo las columnas que el API puede llenar de verdad (SKU de orden, fecha, ítems, total, estado) — se eliminan cliente, tabs de estado, filtro por rango de fechas y borrado de órdenes, que no tienen dato ni endpoint detrás. (b) El detalle se diseña como **superficie de evidencia para el evaluador**: clave de idempotencia, referencia del cobro simulado, motivo del rechazo, y el precio congelado por línea contrastado contra el precio actual del producto, que es la prueba visible de la garantía de `order-placement`. (c) Purga del resto del template: `sections/order/`, `types/order.ts`, `_mock/` completo, direcciones y tarjetas falsas del checkout, y el footer de minimals.cc. (d) Estados vacíos honestos en la tienda: "aún no hay productos" (catálogo vacío) es distinto de "sin resultados" (filtro o búsqueda sin coincidencias). | closed | [real-orders-dashboard](../openspec/changes/archive/2026-08-31-real-orders-dashboard/) |

| TK-049 | Órdenes v2: invalidar caché al comprar, dirección persistida y búsqueda (BE + FE) | requerimiento | Tres hallazgos del usuario al probar TK-048. (a) **Bug de caché**: comprar descuenta el stock en Postgres dentro de la transacción, pero `orders.service` no invalida la caché Redis de `GET /products` (TTL 300 s) como sí hacen `products.service` en create/update/remove y el import. Resultado: la tienda y la lista del dashboard muestran el stock anterior hasta cinco minutos después de una compra, y la app **parece** no descontar inventario. Es deuda de TK-038 que TK-010 no contempló. (b) **Dirección de entrega**: el checkout la recoge pero `CreateOrderDto` solo acepta `items` e `idempotencyKey`, así que nunca llega al API y el detalle de la orden no puede mostrarla — pedirla sin guardarla es teatro. Se persiste como parte del pedido (columnas + migración), corrigiendo la exclusión de envío de initial.md §9 a petición del usuario. (c) **Búsqueda y filtros**: `OrderFiltersDto` solo tiene `page`/`limit`, así que la tabla no tiene buscador; resolverlo en cliente filtraría solo la página visible y mentiría con paginación server-side (el mismo bug que TK-036 corrigió en productos). Se añaden `q` (prefijo de id de orden **y** SKU/nombre dentro de las líneas, vía JOIN a `order_items`), `status` y rango de fechas, más el motivo del rechazo visible en la tabla como tooltip del badge FAILED (solo FE). | closed | [orders-search-and-address](../openspec/changes/archive/2026-08-31-orders-search-and-address/) |
| TK-050 | Revisión de seguridad previa a la entrega (BE) | técnico | Hallazgos de una revisión de toda la superficie, no solo del último cambio. (a) **`POST /auth/sign-up` era público**: como no hay roles, una cuenta solo sirve para administrar el catálogo, así que cualquiera que encontrara el endpoint se daba de alta y leía todas las órdenes con su dirección de entrega. TK-034 escondió el registro en la UI; el API seguía abierto. Deja de ser `@Public()` y el spec de frontera lo afirma. (b) **`GET /users` devolvía los hashes bcrypt**: el `@Exclude()` de la entidad solo aplica cuando algo la serializa, y el resultado de un query builder no lo hace — `findOne` pasaba por `instanceToPlain` y `findAll` no, así que la lista filtraba lo que el detalle ocultaba. (c) **`PATCH /users/:id` guardaba la contraseña en claro**, dejando el sign-in comparando un hash contra texto plano. (d) **El refresh token era un access token**: mismo secreto y mismo payload, solo con siete días de vida y sin nada que lo revocara; se deja de emitir. (e) **`JWT_SECRET` era un placeholder commiteado** (`changeme` en el compose): una clave de firma pública. Ahora un valor débil detiene el arranque y uno ausente se genera por proceso. (f) Menores de la misma pasada: `idempotencyKey` pasa a `@IsUUID()` —comprar no requiere sesión y reproducir una clave devuelve la dirección de esa orden, así que una clave adivinable era una forma de leer la de otro—, techo y validación de enteros en `PaginationDTO` (`page=-1` producía un OFFSET negativo y un 500 en vez de un 400), límite de intentos en `sign-in`, y trim + rechazo de HTML en los campos de envío, como ya hacían los de producto. | closed | — |
| TK-051 | Guía de pruebas manuales por módulo, en español | técnico | Los casos manuales de `docs/testing/` estaban en inglés, cubrían solo el pipeline de importación más un TC-05 de compra, y ese TC-05 había quedado desactualizado: documentaba un `POST /orders` sin `shippingAddress` y con claves de idempotencia tipo `race-buyer-a`, que tras TK-049/TK-050 devuelven `400` en vez de `201`/`409` — el documento describía un contrato que la API ya rechazaba. **Decisión**: traducir y extender la capa in situ en vez de crear una carpeta paralela (`docs/manual/`), porque dos matrices que se desincronizan es exactamente cómo TC-05 quedó obsoleto. (a) `README`, `STRATEGY`, `MATRIX` y `TC-01`–`TC-05` traducidos al español, coherente con el resto de `docs/`; el `README` raíz sigue en inglés. (b) `TC-05` corregido con los payloads reales del DTO vigente. (c) Tres casos nuevos: `TC-06` concurrencia y condiciones de carrera (diez escenarios reproducibles — carrera por la última unidad, sin filas `PENDING` residuales, clave de idempotencia en paralelo resuelta por el índice único, interbloqueo de pedidos multilínea, cantidades fusionadas por producto, reintento de clave rechazada, borrado con FK `RESTRICT`, Redis caído sin cancelar la venta, doble clic en la UI), `TC-07` login y matriz de permisos público/protegido, `TC-08` status y degradación de dependencias. (d) `MATRIX` sube de 76 a 99 casos, con proceso nuevo `P-09` para status/observabilidad. | closed | — (documentación) |
| TK-052 | Restaurar la búsqueda de páginas del dashboard (⌘K) | requerimiento | Petición explícita del usuario: quiere el buscador de páginas del header funcionando. **Revierte dos decisiones previas y hay que decirlo**: TK-027 lo eliminó junto con el resto del chrome de plantilla, y TK-036 descartó revivirlo con el argumento de que "corría sobre mocks; hacerla de verdad es búsqueda cross-entity, otro ticket". Ese argumento aplica a la búsqueda de **contenido** (productos, ordenes), no a la de **páginas**: los items del nav (`config-nav-dashboard`) son datos reales de la app, no mocks, así que el componente restaurado busca sobre el nav vigente (Product, Orders, Status) y no necesita ningún endpoint nuevo. La búsqueda cross-entity sigue fuera de alcance. Se recupera `layouts/components/searchbar/` desde e66765e^ (361 líneas, tres archivos) y se re-cablea en el slot `rightAreaStart` del header, que ya recibe el `navData` real en `dashboard/layout.tsx`. **Dos dependencias del original ya no existen**: el paquete `autosuggest-highlight` y `hooks/use-event-listener.ts`, ambos eliminados en la limpieza de archivos muertos; el resaltado de coincidencias se reimplementa como helper local con tests, y el atajo de teclado con un `useEffect` propio, para no resucitar una dependencia npm que se acaba de quitar. | closed | [restore-dashboard-page-search](../openspec/changes/archive/2026-08-31-restore-dashboard-page-search/) |
| TK-053 | Rango de fechas por el día del visitante y búsqueda por destinatario (BE + FE) | técnico | Detectado por el usuario probando `/dashboard/order`. (a) **Bug de zona horaria**: el filtro comparaba límites de día en **UTC** mientras la columna `Date` pinta hora local, así que una orden creada el 31/08 a las 21:02 (04:02Z del 01/09) se listaba bajo el 1 de septiembre y desaparecía de un rango que terminaba el 31. El FE acota ahora el día elegido en la zona del visitante y manda instantes ISO; el API compara instantes (`createdAt <= :dateTo`) y sigue aceptando fecha sola para Swagger. (b) **Rango invertido**: `dateFrom > dateTo` respondía 400 y el `<Alert>` sustituía a los filtros, de modo que no había forma de corregir las fechas y "Retry" repetía la misma petición — bucle sin salida. Los date pickers se acotan entre sí, la query no se dispara y el fallo se muestra **bajo** la toolbar. (c) Búsqueda por nombre y teléfono del destinatario y columna `Customer`: un UUID no es lo que tiene a mano quien contesta el teléfono. | closed | — (commit 10cefd5) |
| TK-054 | Datos de entrega completos en la orden: email, dirección y tabla legible (BE + FE) | requerimiento | Petición del usuario tras TK-053. (a) **Email**: el formulario de New Address no lo pide, así que la orden no tiene forma de contactar a quien compró. Se añade al formulario como campo **obligatorio** (validación zod + `@IsEmail` en el DTO), se persiste en la orden (columna + migración; nullable en BD porque las órdenes ya existentes no lo tienen, el requisito lo impone el DTO, mismo criterio que la dirección en TK-049) y se muestra en el detalle. (b) **Tabla de órdenes**: hay mucho espacio muerto y `Customer` mezcla nombre y teléfono en una celda. El teléfono pasa a **columna propia** mostrando la bandera/lada del país como el `Field.Phone` del formulario, y se muestran **todos los campos de New Address** (dirección, ciudad, estado, CP, país) además de email. (c) **Filtros**: `q` debe cubrir también los campos nuevos (email y los de dirección), no solo id/nombre/teléfono/SKU/producto. Depende de TK-053. | closed | [order-delivery-details](../openspec/changes/archive/2026-08-31-order-delivery-details/) |
| TK-055 | Revalidar el carrito contra el catálogo antes de cobrar (FE) | requerimiento | Detectado por el usuario: agregó productos al carrito, cambió el precio en el catálogo, recargó, y tanto el mini-carrito como el checkout siguieron mostrando los valores viejos. El carrito vive en `localStorage` (`app-checkout`) con precio y stock **congelados** al agregar, y nada los vuelve a contrastar. **No es solo un hueco, incumple un requisito ya escrito**: `order-history` prohíbe mostrar durante la compra un total distinto del que se registrará, y como `POST /orders` no acepta precios —el servidor los toma del catálogo— confirmar cobra el precio nuevo mientras la pantalla mostró el viejo. Casos que se comportan distinto y hay que tratar por separado: (a) **precio** cambia lo que se paga → se actualiza y se marca la línea con el anterior tachado; (b) **stock** insuficiente → se ajusta la cantidad al disponible y se avisa, en vez de descubrirlo con el `409` del API; (c) **producto borrado** → hoy es un `404 Product X not found` que el checkout pinta como error genérico sin decir qué línea sobra, y pasa a marcarse en la propia línea con acción de quitarla; (d) **nombre y SKU** no cambian lo que se paga —la orden los copia del catálogo al comprar, no del carrito— así que se actualizan con una nota discreta en la línea, sin banner de alerta. Más un aviso agregado ("N productos cambiaron desde que los agregaste") en el mini-carrito y en el paso de pago. **Decisión**: revalidar desde el FE con `GET /products/:id` sobre las líneas del carrito, sin endpoint nuevo — el precio ya es dato público del catálogo; un `POST /orders/quote` se justificaría solo con descuentos o impuestos, que no existen. Requiere guardar el `sku` en `ICheckoutItem`, que hoy no lo lleva y sin él la línea no es identificable cuando el nombre cambia. El disparador realista en esta app es el import CSV, que hace upsert por SKU y puede renombrar y repreciar el catálogo entero de una pasada. | closed | [cart-revalidation](../openspec/changes/archive/2026-08-31-cart-revalidation/) |

## Flujo de trabajo

1. Tomar un ticket `open` → `/opsx:propose TK-### <slug>` (el workflow lee este documento).
2. Revisar proposal/specs/design/tasks generados en `openspec/changes/<slug>/`.
3. `/opsx:apply` para implementar → commits referenciando el ticket (`feat: ... (TK-009)`).
4. `/opsx:archive` al terminar → marcar aquí `closed` y enlazar el change archivado.

Orden sugerido: TK-011 (base de integración) → TK-007 + TK-013 (CRUD + migraciones) → TK-008 (búsqueda) → TK-009 + TK-023 (import CSV + validaciones) → TK-012 (seed) → TK-010 + TK-022 (órdenes + fake payment) → TK-014/015 (hardening, transversal) → TK-016 (tests) → TK-017 (README).
